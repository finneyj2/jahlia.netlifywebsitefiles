# run checks on Markdown writing through proselint script:
# Markdown


before_install:
  - pip install proselint
  - gem install mdl
notifications: 
  - email: never
script: 
  - mdl README.md 
  - proselint README.md

